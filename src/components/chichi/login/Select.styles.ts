import styled from "styled-components"
import IconPath from "../../../assets/iconPath"

const Container = styled.div``

const Chevron = styled(IconPath.Chevron)``

const S = { Container, Chevron }

export default S
