import styled from "styled-components"

const Container = styled.ul`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 24px 8px;
  padding: 16px;
`

const S = { Container }

export default S
